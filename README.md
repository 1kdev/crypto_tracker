# 🚀 Crypto Alert Bot

Telegram-бот для отслеживания цен на криптовалюту и получения персональных уведомлений. Бот получает актуальные данные через публичный API Binance и позволяет каждому пользователю самостоятельно управлять своим списком тикеров.

## 🛠 Tech Stack

* Python 3.10+
* aiogram 3.x — асинхронный фреймворк для Telegram-ботов
* aiosqlite — работа с базой данных SQLite
* Binance API — получение актуальных цен криптовалют
* python-dotenv — загрузка переменных окружения

## ⚙️ Как запустить у себя

### 1. Скачай проект

```bash
git clone https://github.com/1kdev/crypto_tracker.git
cd crypto_tracker
```

### 2. Создай виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установи зависимости

```bash
pip install "aiogram<4.0" python-dotenv aiosqlite pydantic-settings
```

### 4. Настрой Telegram-бота

Создай файл `.env` в корневой папке проекта:

```env
BOT_TOKEN=твой_токен_от_BotFather
```

### 5. Запусти бота

```bash
python app.py
```

База данных SQLite создаётся автоматически при первом запуске.

## ✅ Что работает в версии 1.4.1

### 💼 Мои тикеры

Пользователь видит свои тикеры через интерактивное inline-меню.

Для каждого тикера доступны отдельные действия:

* 📊 просмотр тикера и текущей цены
* ✏️ изменение тикера
* ❌ удаление тикера
* 🔔 настройка уведомлений
* ↩️ возврат назад

Все тикеры хранятся отдельно для каждого пользователя.

### ➕ Добавление тикеров

Пользователь может добавить торговую пару в любом привычном формате — бот сам приводит её к формату биржи:

```text
BTC
BTCUSDT
BTC/USDT
BTC-USDT
BTCUSD
```

Бот проверяет существование пары и не позволяет добавить один и тот же тикер повторно. При добавлении доступна кнопка «↩️ Отмена», а после успешного добавления клавиатура бота гарантированно всплывает снизу (даже на iOS).

### ✏️ Изменение и удаление

Тикеры можно изменить или удалить непосредственно через интерактивное меню. При удалении доступна кнопка «↩️ Отмена», если пользователь передумал.

Все действия выполняются для конкретного пользователя и не затрагивают тикеры других пользователей.

### 🔔 Уведомления

Для каждого тикера можно настроить персональные уведомления.

Доступны интервалы:

* 5 минут
* 15 минут
* 30 минут
* 45 минут
* 60 минут

Также можно настроить уведомление при изменении цены.

Доступные значения:

* 0.5%
* 1%
* 2%
* 3%
* 5%
* 10%
* собственное значение в процентах

Каждое уведомление явно указывает свой тип и период сравнения, чтобы пользователю было понятно, что именно он получил:

```text
⏱ Периодическое оповещение · каждые 15 мин
ETHUSDT
2 407.36 USDT
📉 -0.12% | -3.00 USDT (с прошлого оповещения)
```

Доступна глобальная пауза всех уведомлений и отдельная пауза ежечасного дайджеста — настройки и тикеры при этом не удаляются.

Настроить оповещения для конкретного тикера можно двумя путями: через «💼 Мои тикеры» → тикер → 🔔, либо через «⚙️ Настройки» → «🔧 Настроить оповещения тикера» — второй путь ведёт сразу к экрану периода/% изменения, минуя карточку редактирования.

### 📈 Автоматические обновления и статистика

Бот автоматически отправляет пользователю актуальную цену отслеживаемых тикеров согласно настроенным уведомлениям.

Также предусмотрены автоматические часовые обновления цены.

При открытии карточки конкретного тикера показывается статистика изменения цены за последние 24 часа.

Отдельная кнопка «📊 Статистика» в главном меню выводит одним сообщением все отслеживаемые тикеры с ценой и изменением за 24ч.

### 🗄 База данных

Используется SQLite.

В базе хранятся:

* пользователи Telegram
* тикеры пользователей
* настройки уведомлений
* данные, необходимые для контроля изменений цены

Отдельный сервер базы данных не требуется.

### 🔐 Разделение пользователей

Каждый пользователь работает только со своими тикерами и настройками уведомлений.

Операции добавления, изменения, удаления и настройки алертов привязаны к Telegram ID пользователя.

## 📁 Структура проекта

```text
crypto_tracker/
├── app.py
├── handlers/
│   └── routes.py
├── forms/
│   └── ticker.py
├── db_main/
│   └── database.py
├── kbds/
│   └── reply.py
├── .env
└── telegram.db
```

## 🚧 План развития

* расширение списка поддерживаемых бирж
* дополнительные типы уведомлений
* улучшение управления настройками
* расширение статистики по отслеживаемым активам
* дополнительные настройки пользователя

## 📝 Changelog

### v1.4.1

**Тикеры и UX**
* Умная нормализация тикера: `BTC`, `BTCUSDT`, `BTC/USDT`, `BTC-USDT`, `BTCUSD` — приводятся к формату биржи автоматически
* Кнопка «↩️ Отмена» при добавлении и при удалении тикера
* Исправлено: клавиатура бота теперь гарантированно всплывает снизу после добавления тикера (раньше на iOS оставалась открытой системная клавиатура ввода)
* Из списка «💼 Мои тикеры» убраны иконки — остались только названия

**Уведомления и статистика**
* Во все типы уведомлений (периодические, по % изменения, часовые) добавлена статистика изменения цены
* Заголовки уведомлений теперь явно называют тип оповещения и период сравнения
* Добавлена статистика за 24ч в карточку тикера
* Новая кнопка «📊 Статистика» в главном меню — одно сообщение со всеми тикерами и их изменением за 24ч (один batch-запрос к Binance)
* Исправлен баг с двойным минусом при отрицательном изменении в 24ч-статистике
* Глобальная пауза всех уведомлений и отдельная пауза часового дайджеста

**Настройки**
* Новый путь «⚙️ Настройки» → «🔧 Настроить оповещения тикера» — сразу открывает экран периода/% изменения выбранного тикера, минуя карточку редактирования
* Кнопка «⬅️ Назад» из этого пути возвращает в «⚙️ Настройки»; поведение «💼 Мои тикеры» не изменилось

**База данных**
* Безопасная миграция через `ALTER TABLE ADD COLUMN` (без потери данных):
  * `users.notifications_enabled`, `users.hourly_enabled` (DEFAULT 1)
  * `user_tickers.last_periodic_price`, `user_tickers.last_hourly_price` (REAL)

## 📌 Version

**v1.4.1**

*Developed by 1kdev*